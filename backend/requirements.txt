# Database & ORM
sqlalchemy>=2.0.0
python-dotenv>=1.0.0

# PostgreSQL (required when DATABASE_URL uses postgresql+psycopg2://...)
psycopg2-binary>=2.9.9

# Optional: MySQL / MariaDB
# pymysql>=1.1.0

# SQLite is built into Python — no extra driver needed for local dev

# API framework & validation
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
email-validator>=2.0.0
